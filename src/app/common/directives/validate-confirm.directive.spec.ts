import { ValidateConfirmDirective } from './validate-confirm.directive';

describe('ValidateConfirmDirective', () => {
  it('should create an instance', () => {
    const directive = new ValidateConfirmDirective();
    expect(directive).toBeTruthy();
  });
});
